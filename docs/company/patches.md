# Patch log

Every touch point in stock Gitea source, however small. Check this list
after each upstream rebase/merge — everything else in `docs/company/` is
config or additive files and needs no review.

| File | What | Why | Status |
|---|---|---|---|
| `routers/web/web.go:366` | `company.RegisterRoutes(m)` inside `registerWebRoutes` | Attach the user-facing POST endpoints. **Mounted here, not `routers/init.go`** as [mount-points.md](mount-points.md) originally described — inside `registerWebRoutes` it inherits the session/auth middleware, which the routes need | done |
| `routers/web/web.go:914` | `company.RegisterAdminRoutes(m)` inside the existing `/-/admin` group | Cross-org activity view in the admin panel; inherits `adminReq` | done |
| `routers/web/web.go:315` | `mid = append(mid, company.GateNonAdminUI)` in `Routes()`, right after the auth middleware is appended | Default-deny whitelist so non-admins can't reach native Gitea screens by typing a URL — see [ui-gate.md](ui-gate.md) | done |
| `routers/web/web.go` | `company.RedirectToWorkspaceIfEmpty` inserted into the bare `/{username}/{reponame}` route's middleware chain, right before `repo.Home` | Send a brand-new empty repo straight to the workspace editor instead of Gitea's native "empty repository" git-clone-instructions page — see `company/workspace.go` | done |
| `templates/base/head.tmpl` | `?v=2` query on the two favicon `<link>` hrefs | Browsers cache favicons per-origin and never refetch on reload — origins visited before the custom AUMOVIO favicon landed (e.g. `0.0.0.0:3000`) kept showing the stock Gitea icon forever; changing the URL forces one refetch. Bump the number if the icon artwork ever changes again | done |
| `web_src/js/features/notification.ts` | Both background-poll `GET` calls now send `X-Gitea-Fetch-Action: 1` | Without it, a session that expires between polls gets 303'd to `/user/login?redirect_to=/notifications/...`; the login page's GET handler stashes that into the `redirect_to` cookie (`modules/web/middleware/cookie.go`), so the *next* real login silently lands on the raw notifications JSON/HTML fragment instead of wherever the person actually was — this is what "로그인하면 이쪽으로 리다이렉팅 될 때 있어" was. Upstream bug, not something we introduced | done |
| `routers/web/web.go` | `company.SetDeployRequestPageData` inserted into the native `/{owner}/{repo}/pulls/{index}` view's middleware chain, right before `repo.ViewIssue`, and again before `repo.ViewPullMergeBox` on `/merge_box` (the box reloads itself while a request is being checked, and the label has to come with it) | Sets the template data a Deploy Request PR page needs: the "AI 코드 리뷰" button (`company/pull_ai_review.go`) and the company sidebar — requester, permissions, packages, the read-only assistant (`company/deploy_review.go`). Deploy Request PRs on the central deploy repo only, admin-only | done |
| `routers/web/repo/pull_merge_form.go` | When `ctx.Data["CompanyMergeLabel"]` is set (a Deploy Request PR), the "merge" style's button text is that label ("Approve deploy") and it is the only style offered | The person approving is not a developer; "Create merge commit / Rebase / Squash" is a choice they cannot make and should not be asked to. Set by `company.SetDeployRequestPageData` | done |
| `routers/web/web.go` | `company.RedirectPullsToCentral` inserted ahead of `user.Pulls` on `/pulls` | The navbar entry is "Deploy approvals" (`custom/templates/base/head_navbar.tmpl`), and for an administrator it lands on the central repository's pull request list: the global list only shows repositories one is a member of, and an administrator is not a member of the central repository, so the deploy requests were exactly what it left out | done |
| `routers/web/web.go` | `GET /activity` → `company.SetActivityLogPage`, then `Home` | The feed of every push, which an administrator's dashboard no longer shows inline, on its own page behind an "Activity log" button (`custom/templates/user/dashboard/dashboard.tmpl`) | done |
| `routers/web/web.go` | `company.SetDashboardApps` and `company.SetDashboardDeploys` inserted ahead of `Home` on `/` | An administrator's dashboard lists the recent deploy requests — who asked, who approved, how it went — above the feed, and the feed override leaves out the platform's own actions on the central repository, which said the same things as branch names and pushes | done |
| `routers/web/repo/view_home.go` | `checkOutdatedBranch` resyncs the branch table from git (`repo_module.SyncRepoBranches`) when a branch's recorded commit differs from the real one, and only warns if that fails | The native page told a department "the Git hooks seem broken, follow the documentation and push" — a sentence nobody here can act on, about a table Gitea can fix itself | done |
| `routers/web/repo/setting/setting.go` + `routers/api/v1/repo/repo.go` | `company.RepoDeleteBlocked` checked at the top of both repository delete handlers | A repository whose app is still on the platform cannot be deleted; removal is a request an administrator approves (`company/appremoval.go`), and only then may the code go | done |
| `routers/api/v1/api.go` | `company.GuardDeployApprovalAPI` inserted into `POST /api/v1/repos/{owner}/{repo}/pulls/{index}/merge`, before `repo.MergePullRequest` | The API is a second door to the same merge; a token with write access to the central repository must not approve a deploy request either | done |
| `routers/web/web.go` | `company.GuardDeployApproval` inserted into `POST /{owner}/{repo}/pulls/{index}/merge`, after the form binding and before `repo.MergePullRequest` | For a Deploy Request: refuses the merge unless the approver is a site administrator (write access to central-deploy is not enough), logs who approved, and records the packages ticked on the sidebar as part of the request so the merge writes them to apps.yml with everything else. Other pull requests pass straight through | done |
| `templates/repo/issue/view_content.tmpl` | Full `custom/` override adding one conditional `<form>` (the AI review button) into the native comment/status-button footer, swapping the sidebar for `company/deploy_review_sidebar` when `.IsDeployRequest`, and showing the opening post as the requester's (`.DeployRequester`, message without the "Requested by" line) | Same feature as above — the button itself; `.ShowDeployAIReview`/`.DeployAIReviewURL` come from the middleware entry just above. The sidebar swap hides reviewers/milestone/assignees/due date/dependencies from the non-developer approving a deploy, and shows what decides it instead | done |
| `routers/init.go` | One `company.InitAppPlatform()` call added at the end of `GlobalInitInstalled`, just before `cron.Init(ctx)` | Starts the deployed-app platform: probes the sandbox, launches the deploy workers, and reconciles apps that were running before the restart. It cannot be a package `init()` — it needs settings and the database, which are only up by this point. One call rather than several so an upstream merge has one line to reconcile, not four — see `company/appinit.go` | done |
| `routers/web/web.go` | Three routes added next to the existing `/deploy` ones: `GET /{owner}/{repo}/_app`, `POST …/_app/env`, `POST …/_app/{verb}` | The department's own app screen — start/stop/restart/rollback, environment variables, access mode. Named `_app` rather than `_deploy` because `/deploy` already exists one underscore away; the prefix follows the existing `_edits`/`_edits_ai` convention. Reuses the group's `reqRepoCodeWriter` for writes | done |
| `routers/web/web.go` | `company.SetAppPermissionData` inserted into the `/{username}/{reponame}` repo-home middleware chain, after `RedirectToWorkspaceIfEmpty` | Supplies the repository sidebar's app panel — status, what the app is and is not allowed to do, and why. Reads only in-memory state, so it adds no query or file read to the most-visited page. Same injection pattern as the two entries above | done |
| `templates/admin/navbar.tmpl` | Full `custom/` override adding one `<details>` group with two links (app deployments, activity) | The admin menu had no entry for either company screen, so `/-/admin/company-activity` could only be reached by typing its URL. Gitea has no partial-template insertion, so a full copy is the only option — accepted here because this file is a list of links with almost no Go-symbol surface, and the worst drift outcome is a missing upstream menu item rather than a broken page | done |
| `cmd/main.go` | `company.NewDeptAppExecCommand()` added to `subCmdStandalone` | Registers the hidden `deptapp-exec` subcommand that department apps are started through on hosts where bubblewrap cannot run. It has to be a subcommand of this binary because Landlock and seccomp must be entered between fork and exec, and Go's `os/exec` has no hook there — the child locks itself down and then `execve`s the app. Listed as standalone because it reads no config and opens no database | done |
| `routers/web/web.go` | `company.SetDashboardApps` inserted into the two `/{org}/dashboard` routes | Supplies the dashboard's deployed-app panel. A repository is not an app — most never become one, and the list says nothing about whether the ones that did are up — so "is our thing running?" took opening repositories one at a time. Same injection pattern as the entries above; scoped to organizations the viewer already belongs to | done |
| `templates/user/dashboard/dashboard.tmpl` | Full `custom/` override adding one line after the repo list | Renders the panel above. A 17-line structural file with no Go-symbol surface, which is why a full copy is acceptable here — the drift check (`docs/company/scripts/check-template-drift.sh`) covers it either way | done |
| `routers/web/admin/users.go` | `company.AdminUserPanel(ctx)` inserted into `EditUser` | Puts department membership and storage on the page an administrator is already on. Membership normally arrives with the login group sync, which is the wrong mechanism for someone's first week — and the admin looking at a person who belongs to no department is exactly who can fix it. Reads only `ctx.Data`, so it adds nothing to a core signature | done |
| `templates/base/head_navbar.tmpl` | Full `custom/` override; both "Help" links now read `.CompanyHelpURL` | Gitea's Help goes to Gitea's documentation, which answers questions nobody in a department is asking. The link is whatever an administrator set, and absent when they set nothing — better than sending an employee somewhere that cannot help them | done |
| `templates/user/auth/signin_inner.tmpl` | Full `custom/` override dropping the "forgot password" link | Accounts are provisioned by the directory and not owned here, so the link led to a page that ends in "ask an administrator" — and with mail off it ended there without explaining why | done |
| `routers/web/admin/users.go` | `company.RefuseAccountDeletion(ctx)` at the top of `DeleteUser` | Accounts come from the directory, so deleting one here removes the Gitea-side record while the person still exists upstream and the next login recreates them, emptied of their membership. Deactivation is what ends access. The button is gone from the page; this is the half that holds when somebody types the URL | done |
| `templates/admin/user/list.tmpl` | Full `custom/` override adding an "account from" column | LDAP and local accounts were indistinguishable in the list, and which one a row is changes what an administrator may do with it | done |
| `cmd/web.go` | `company.StopAllApps()` after `<-graceful.GetManager().Done()` in `serveInstalled` | Stops every department app, with SIGTERM and its grace period, as Gitea exits. Without it an app outlived Gitea — unwatched until the next start, which then SIGKILLed it mid-request. Not a graceful-manager hook: `installSignals` (`cmd/helper.go`) cancels the manager's parent context on SIGTERM, so `Done()` closes at once and terminate hooks never get to run. This line is the one point every exit passes | done |
| `Makefile` | `company` added to `GO_DIRS` | Without it `GO_SOURCES` misses the whole package, so `make backend` finds no newer prerequisite and rebuilds nothing after a change in `company/` — the edit looks like it did not take, and a stale binary keeps serving. One word on one line | done |
| `templates/admin/user/edit.tmpl` | Full `custom/` override appending one block before the footer | Renders the panel above. Upstream's file is copied unchanged and the addition is fenced by a comment, so the drift check (`docs/company/scripts/check-template-drift.sh`) has a clean boundary to compare against | done |
| `templates/status/500.tmpl` | Full `custom/` override: department-facing wording, admin-only error text, no GitHub issue link | Upstream hands the Go error text, the version and a "file a bug on github.com" link to whoever hit the page. The person who hits a 500 here is usually a department member mid-upload: none of that is actionable for them, and the link points at software they do not run on a site the company does not allow. They get what to do and who to tell; an administrator still gets the error, folded away. Same audience split as `company/usererror.go`. Upstream's partial-render fix-up script is kept verbatim | done |

Several of these are files that only change when Gitea's own routing skeleton
changes — rare, and each is a self-contained `append`/call, easy to
re-locate even if the surrounding lines shift. The template override is a
full-file copy (Gitea's overlay mechanism has no partial-template diffing),
so it needs re-diffing against upstream on every rebase that touches
`templates/repo/issue/view_content.tmpl` — the same tradeoff already
accepted for `custom/templates/repo/view_content.tmpl` and the fully custom
`workspace.tmpl`/`deploy.tmpl`.

Nothing else is planned. Config (`app.ini`), `custom/` overrides, and new
files under `company/` are not listed here — they don't touch tracked core
source, so an upstream rebase never has anything to reconcile for them.
`APP_NAME` in `custom/conf/app.ini` is what controls the "Gitea: Git with a
cup of tea" suffix in page titles — set to a real, non-empty product name
rather than blank, since `go-ini`'s `MustString` can't tell "explicitly
blank" from "absent" apart and would've silently kept the default either
way, which would've needed a core patch to work around.
